*/ el formulario 

 <div class="login-container">
        <h2>Iniciar Sesión</h2>
        <form action="#" method="POST">
            <div class="input-group">
                <label for="email">Correo electrónico</label>
                <input type="email" id="email" name="email" required>
            </div>

            <div class="input-group">
                <label for="password">Contraseña</label>
                <input type="password" id="password" name="password" required>
            </div>

            <button type="submit">Ingresar</button>

            <div class="extra-links">
                <a href="#">¿Olvidaste tu contraseña?</a>
            </div>
        </form>
    </div>

/*

*/ estilo del formulario

        .login-container {
            background: white;
            padding: 30px;
            width: 320px;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgba(0,0,0,0.15);
        }

        .login-container h2 {
            text-align: center;
            margin-bottom: 25px;
        }

        .input-group {
            margin-bottom: 15px;
        }

        .input-group label {
            display: block;
            margin-bottom: 5px;
            font-size: 14px;
        }

        .input-group input {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        button {
            width: 100%;
            padding: 12px;
            background: #007bff;
            border: none;
            color: white;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background: #0056c4;
        }

        .extra-links {
            text-align: center;
            margin-top: 15px;
        }

        .extra-links a {
            color: #007bff;
            text-decoration: none;
            font-size: 14px;
        }

        .extra-links a:hover {
            text-decoration: underline;
        }
/*

*/ menú desplegable

 <div class="dropdown">
  <img src="ruta/a/tu/imagen.jpg" alt="Descripción de la imagen">
  <div class="dropdown-content">
    <ul>
      <li><a href="#">Opción 1</a></li>
      <li><a href="#">Opción 2</a></li>
      <li><a href="#">Opción 3</a></li>
    </ul>
  </div>
</div>
/*

*/estilo del menú desplegable
.dropdown {
     position: relative;
    display: inline-block; /* Para que la imagen y el desplegable se comporten como un solo elemento */
        }
    .dropdown-content {
  display: none; /* Oculta el menú por defecto */
  position: absolute;
  background-color: #f9f9f9;
  min-width: 160px;
  box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
  z-index: 1;
    }

    .dropdown-content ul {
  list-style: none;
  padding: 0;
  margin: 0;
    }

    .dropdown-content li a {
  color: black;
  padding: 12px 16px;
  text-decoration: none;
  display: block;
    }

    .dropdown-content li a:hover {
  background-color: #9b5454;
    }

    /* Muestra el menú desplegable cuando el usuario pasa el ratón por encima del div principal */
    .dropdown:hover .dropdown-content {
  display: block;
    }
/*

protección de algunas paginas para inicio de sesion
*/const sesion = JSON.parse(localStorage.getItem("sesionActiva"));
if (!sesion || sesion.rol !== "admin") {
    alert("Acceso no autorizado");
    window.location.href = "../pages/login.html";
}
/*

npx http-server

JSON.parse(localStorage.getItem("sesionActiva"));